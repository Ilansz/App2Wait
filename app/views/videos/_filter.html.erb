<%= form_tag videos_path, method: :get, remote: true do %>
  <%= select_tag :query,
    options_from_collection_for_select(@challenges, 'id', 'name'), prompt: "Select a challenge",
    class: "form-control"
  %>
  <%= submit_tag "Find", name: nil, class: 'btn btn-primary' %>
<% end %>
